{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The narrowest set of permissions the UI actually uses. Everything the app does with the filesystem and the network goes through the engine's own commands, which are auditable Rust; the plugin surface is deliberately limited to the two things a web view cannot do for itself — pick a folder, and hand a path to the shell.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "dialog:allow-open",
    "opener:allow-reveal-item-in-dir",
    {
      "identifier": "opener:allow-open-path",
      "allow": [{ "path": "**" }]
    }
  ]
}
